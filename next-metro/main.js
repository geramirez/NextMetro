const { NextMetro } = require('./src/NextMetro')
const { Prediction } = require('./src/prediction')
const { PredictionsService } = require('./src/predictionsService')

module.exports = {
  NextMetro,
  Prediction,
  PredictionsService
}
